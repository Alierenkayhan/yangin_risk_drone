# DroneCommand AI — Frontend Environment Variables
# cp .env.example .env.local

# Django Backend
VITE_API_BASE_URL=http://localhost:8000/api
VITE_API_BACKEND_URL=http://localhost:8000

# Google Maps (zorunlu — harita görüntüleme için)
VITE_GOOGLE_MAPS_API_KEY=

# OpenAI ChatGPT API (opsiyonel — AI analiz için)
VITE_OPENAI_API_KEY=
VITE_OPENAI_MODEL=gpt-4o-mini

# RabbitMQ Web STOMP (opsiyonel — varsayılan backend'den gelir)
VITE_RABBITMQ_STOMP_URL=ws://localhost:15674/ws
