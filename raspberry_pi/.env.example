# ═══════════════════════════════════════════════
# DroneCommand AI — Raspberry Pi Drone Client
# Yapılandırma Dosyası
# ═══════════════════════════════════════════════

# ── Drone Kimliği ─────────────────────────────
DRONE_ID=D-01
DRONE_NAME=Kartal-1
DRONE_MODEL=DJI Matrice 300

# ── Backend API ───────────────────────────────
# Sunucu IP adresi (Pi'nin erişebildiği)
API_BASE_URL=http://localhost:8000/api

# ── RabbitMQ ──────────────────────────────────
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# ── Kamera ────────────────────────────────────
# picamera2 | usb | test
CAMERA_TYPE=picamera2
CAMERA_DEVICE=/dev/video0
CAMERA_WIDTH=640
CAMERA_HEIGHT=480
CAMERA_FPS=10
JPEG_QUALITY=75

# ── GPS ───────────────────────────────────────
# gpsd | serial | mock
GPS_TYPE=gpsd
GPS_HOST=127.0.0.1
GPS_PORT=2947
GPS_SERIAL_PORT=/dev/ttyAMA0
GPS_SERIAL_BAUD=9600

# ── Sensörler ─────────────────────────────────
# bme280 | dht22 | mock
SENSOR_TYPE=mock
# BME280 I2C adresi
BME280_ADDRESS=0x76
# DHT22 GPIO pini
DHT_PIN=4

# ── Telemetri ─────────────────────────────────
TELEMETRY_INTERVAL=1.0
VIDEO_ENABLED=true
VIDEO_FPS=10

# ── Simülasyon ────────────────────────────────
# Gerçek sensör yokken mock veri üret
MOCK_FIRE_PROBABILITY=0.05
MOCK_SMOKE_PROBABILITY=0.05

# ── Loglama ───────────────────────────────────
LOG_LEVEL=INFO
LOG_FILE=/var/log/dronecommand/drone.log
