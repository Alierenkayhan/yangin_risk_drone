# DroneCommand AI — Backend Environment Variables
# cp .env.example .env

DJANGO_SECRET_KEY=change-me-to-a-random-secret-key
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
LOG_LEVEL=INFO

# Database
DATABASE_ENGINE=django.db.backends.sqlite3
DATABASE_NAME=db.sqlite3
# DATABASE_ENGINE=django.db.backends.postgresql
# DATABASE_NAME=dronecommand
# DATABASE_USER=dronecommand
# DATABASE_PASSWORD=change-me
# DATABASE_HOST=postgres
# DATABASE_PORT=5432

# RabbitMQ
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USERNAME=dronecommand
RABBITMQ_PASSWORD=change-me
RABBITMQ_VHOST=/
RABBITMQ_STOMP_HOST=localhost
RABBITMQ_STOMP_PORT=15674

# Exchanges
RABBITMQ_EXCHANGE_TELEMETRY=drone.telemetry
RABBITMQ_EXCHANGE_COMMANDS=drone.commands
RABBITMQ_EXCHANGE_VIDEO=drone.video
RABBITMQ_EXCHANGE_ALERTS=drone.alerts
RABBITMQ_EXCHANGE_GUI=drone.gui

# YOLO
YOLO_MODEL_PATH=detection/models/fire_smoke.pt
YOLO_CONFIDENCE=0.5
YOLO_IOU=0.45
YOLO_DEVICE=cpu

# Video
VIDEO_FRAME_WIDTH=640
VIDEO_FRAME_HEIGHT=480
VIDEO_FPS=30
VIDEO_JPEG_QUALITY=80

# Server
BACKEND_PORT=8000
BACKEND_WORKERS=2
